import java.util.List;

public class ControleTransacao {
/*    private final Conta conta;

    public ControleTransacao(Conta conta){
        this.conta = conta;
    }

    public boolean validaTransacao(Movimento movimento){
        if(movimento.getTipo().equals(Tipo.DEBITO){
            Double saldoDisponivel = conta.getSaldo(); + contaLimiteChequeEspecial();
            if(saldoDisponivel < moviemnto.getValor()){
                thrw new TransacaoExceprion(movimento, conta);
            }
    }

 */
}
